import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
   footer:{
    display: 'flexWrap',
    minHeight: ' 100px',
    flexDirection: 'column',
    marginTop: '50px',
    
    bottom: 0,
   },
}));